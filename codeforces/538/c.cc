#include <iostream>
#include <vector>
using namespace std;
int main()
{
	long long day_cnt, page_cnt;
	cin >> day_cnt, page_cnt;
	long long pages[page_cnt][2];

	for (long long i = 0; i < page_cnt; ++i)
	{
		cin >> pages[i][0] >> pages[i][1];
	}

	for (long long i = 1; i < page_cnt; ++i)
	{
			
	}
}
